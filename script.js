// JavaScript to manage tab functionality if needed (for dynamic actions)
document.querySelectorAll('.tab-button').forEach(button => {
    button.addEventListener('mouseover', function() {
        // Optionally add some dynamic content loading or actions on hover.
    });
});
